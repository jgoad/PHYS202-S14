#!/bin/bash

# This script configures Git whenever you start working 
# on multiple machines. Run it any time you decide to work with 
# Git on a new computer

git config --global user.name "Jason Goad"
git config --global user.email "jgoad@calpoly.edu"
git config --global color.ui "auto"
git config --global core.editor "nano"
